import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reisenthaler.mymoodlog.app',
  appName: 'MyMoodLog',
  webDir: 'www'
};

export default config;
